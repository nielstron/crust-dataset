use crate::cards::{Card, RANKS_PER_DECK, Rank, SUITS_PER_DECK, Suit};
use rand::seq::SliceRandom;
use rand::thread_rng;
pub const CARDS_PER_DECK: usize = 52;
pub fn new_deck() -> Vec<Card> {
    unimplemented!()
}
pub fn deck_shuffle(deck: &mut Vec<Card>) {
    unimplemented!()
}
