use crate::cards::{Card, new_card_from_chars};
use crate::deck::{deck_shuffle, new_deck};
use crate::hands::{hand_compare, hand_sort};
use rand::thread_rng;
use std::env;
use std::process;
const ITERATIONS: usize = 2_000_000;
fn usage(program_name: &str) {
    unimplemented!()
}
fn main() {
    unimplemented!()
}
